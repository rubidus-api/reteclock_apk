Fixed: while layout slides were playing, the background picture stopped changing. Each time one slide gave way to the next, the clock re-read its settings and started the picture show again at its first picture, so a picture set to hold for, say, an hour never reached the second one. The show now keeps its place across that rebuild, and across a return from the settings screens. Reported in issue #53.
