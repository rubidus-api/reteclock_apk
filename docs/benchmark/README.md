# Benchmark

Benchmark plans, methodology, and summarized results live here.

Put small generated output under `docs/benchmark/results/`. Put large rebuildable artifacts under
`build/` or `resources/cache/`.

Do not record machine names, local absolute paths, user names, IP addresses, ports, or secret
configuration in benchmark notes.
