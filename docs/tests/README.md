# Tests

TDD documentation and executable test organization.

`test-index.md` is the compact catalog; detailed cases live in `cases/`; executable tests live in
`tests/`. Compiled test output goes to `build/tests/`, never into `tests/`.
